Agora vamos criar as tabelas no banco de dados.

pgAdmin

Adicionar os campos na tabela.
ALTER TABLE usuario ADD COLUMN cep character varying(200);

Precisamos mudar todas as queries do DAO que ainda usam as strings sem os atributos novos(de cep).